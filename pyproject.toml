[project]
name = "lakehouse-content"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "google-api-core",
    "google-auth",
    "google-cloud-bigquery",
    "google-cloud-dataplex",
    "google-cloud-storage",
    "protobuf",
    "pydantic>=2.0",
    "pyyaml",
    "requests",
    "typer",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-cov",
    "typer[testing]",
]

[project.scripts]
lakehouse = "ingestion.cli:app"
